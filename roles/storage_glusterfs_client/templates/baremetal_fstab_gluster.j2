{{ inventory_hostname }}:/gv1 /gv1 glusterfs defaults,backupvolfile-server={{ storage.glusterfs.peers.0.name }}:{{ storage.glusterfs.peers.1.name }},_netdev 0 0
{{ inventory_hostname }}:/gv3 /gv3 glusterfs defaults,backupvolfile-server={{ storage.glusterfs.peers.0.name }}:{{ storage.glusterfs.peers.1.name }},_netdev 0 0
{{ inventory_hostname }}:/glusterfinal /glusterfinal glusterfs defaults,backupvolfile-server={{ storage.glusterfs.peers.0.name }}:{{ storage.glusterfs.peers.1.name }},_netdev 0 0
